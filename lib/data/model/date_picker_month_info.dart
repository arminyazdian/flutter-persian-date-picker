class DatePickerMonthInfo {
  final int offset;
  final int daysWithOffset;
  final int? selectedDay;

  DatePickerMonthInfo({required this.offset, required this.daysWithOffset, this.selectedDay});
}
